1.角色组权限设计
	1.1 操作权限
	1.2 字段查看权限
	1.3 记录查看权限
		设计角色记录权限表，根据不同角色，指定不同表，不同的可查看ID值。
		根据记录类型指定针对哪一个表，目前指定的表就是供应商对象表。

1.UserLookOverPermission里面存在多级菜单，这些不一定需要在表当中定义，大部分定义从数据库读出来之后在内存当中计算。
2.每个表对应自己的表名查找相对应的可查看列。
3.记录权限关联组Id,跟用户无直接关系。
4.增加字段控制特性，销售明细目前可根据用户是否显示字段值来控制是否将字段显示出去。
5.前台需要使用canViewFields()方法来判断字段是否可以显示。
